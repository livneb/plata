{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -m plata.entrypoints",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  },
  "_notes": "Create THREE Railway services from the same repo. Each one sets SERVICE_ENTRYPOINT env var to one of: ingestion_hub | intelligence_sandbox | execution_vault. Bybit secrets MUST be configured ONLY on execution_vault."
}
